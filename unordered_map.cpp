//#include <stdio.h>
#include<iostream>
#include <unordered_map>


using namespace std;


int main()
{
    unordered_map<int , int > umap;
    //unordered_map<int , int>::iterator umap_it;
    umap[0] = 1;
    //umap_it = umap.find(1);
    //cout << umap.find(1) << endl;

    return 0;
}